import React from "react";
import { View, StyleSheet, Text } from "react-native";

const TrakDetailScreen = () => {
  return <Text style={{ fontSize: 48 }}>TrakDetailScreen</Text>;
};

const styles = StyleSheet.create({});

export default TrakDetailScreen;
